
Функция ПолучитьДатуНачалаНедели(Дата) Экспорт
	
	ТекущийДень = День(Дата);
	
	Если ТекущийДень = 1 Тогда
		Возврат Дата;
	Иначе
		КоличествоДней = -1 * (ТекущийДень - 1);	
	КонецЕсли;
	
	Возврат ОбщегоНазначенийВызовСервера.ДобавитьКДате(Дата, КоличествоДней, "ДЕНЬ");
	
КонецФункции